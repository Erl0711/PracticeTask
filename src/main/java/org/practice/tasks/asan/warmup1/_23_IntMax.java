package org.practice.tasks.AsanC.warmup1;

public class _23_IntMax {
    /*
    Given three int values, a b c, return the largest.
     */
    public static int intMax(int a, int b, int c) {
        return Math.max(Math.max(a,b),c);
    }
}
